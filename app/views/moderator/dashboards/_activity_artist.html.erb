<%= table_for(@dashboard.artists) do |t| %>
  <% t.caption { "Artist Updates" } %>
  <% t.column("User") do |activity| %>
    <%= link_to_user(activity.user) %>
  <% end %>
  <% t.column("Count") do |activity| %>
    <%= link_to(activity.count, artist_versions_path(search: { updater_id: activity.user.id })) %>
  <% end %>
<% end %>
