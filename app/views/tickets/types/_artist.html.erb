<tr>
  <td><span class="title">Reported Artist</span></td>
  <td>
    <% if @ticket.model.present? %>
      <%= link_to(@ticket.model.name, artist_path(id: @ticket.model.id)) %>
    <% else %>
      <span class="text-error">Artist has been deleted</span>
    <% end %>
  </td>
</tr>
