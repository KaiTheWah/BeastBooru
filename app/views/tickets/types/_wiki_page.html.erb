<tr>
  <td><span class="title">Reported Wiki Page</span></td>
  <td>
    <% if @ticket.model.present? %>
      <%= link_to(@ticket.model.title, wiki_page_path(id: @ticket.model.id)) %>
    <% else %>
      <span class="text-error">Wiki page has been deleted</span>
    <% end %>
  </td>
</tr>
